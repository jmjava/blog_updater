# Blog Updater config (copy to config.env and edit if needed)
# API server port (must match uvicorn --port and script API_BASE)
API_PORT=8080
# OAuth uses 8080 (same as API). Stop the API before one-time auth.
